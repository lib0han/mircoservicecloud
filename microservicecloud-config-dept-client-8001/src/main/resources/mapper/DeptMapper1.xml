<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.springcloud.dao.DeptMapper1">
    <!--<resultMap id="ss" type="com.demo.springcloud.entities.Dept">
        <id column="deptno" property="deptno" javaType="String"/>
        <result column="db_source" property="db_source" javaType="String"/>
        <result column="db_source" property="dname" javaType="String"/>
    </resultMap>-->
    <select id="findById" resultType="com.demo.springcloud.entities.Dept" parameterType="java.lang.Long">
        select deptno,dname,db_source from dept where deptno=#{deptno}
    </select>
    <select id="findAll" resultType="com.demo.springcloud.entities.Dept">
        select deptno,dname,db_source from dept
    </select>
    <insert id="addDept" parameterType="com.demo.springcloud.entities.Dept">
        INSERT INTO dept(dname,db_source) VALUES (#{dname},DATABASE())
    </insert>
</mapper>